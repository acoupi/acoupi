# Setup on the RPi:

1. Install raspbian OS - 64-bit lite on SDCard

2. Setup RPI with username and password

3. Make variable user and home system directory
export USER=$USER
export HOME=$HOME

4. Check system update and upgrade
sudo apt update 
sudo apt upgrade

5. Install dependencies - git and other packages
sudo apt install git alsa-utils libsndfile1 libasound2-dev wget cmake #pulseaudio

6. Install python3 and python3 libs
sudo apt install python3-dev python3-pip python3-venv python3-pyaudio

7. Move to home directory if not and clone the Acoupi GitHug Repositiory - rpi branch 
cd ~
git clone -b batdetect2-rpi --depth=1 https://github.com/audevuilli/acoupi.git ${HOME}/acoupi

8. Install pyaudio using install_pyaudio.sh script (ignore warnings)
bash $HOME/acoupi/src/acoupi/scripts/install_pyaudio.sh

9. Install libraries packages with pip
pip3 install -U -r $HOME/acoupi/src/acoupi/requirements.txt

10. Setup Microhpone configuration
Plug the USB microphone into the RPi. 
Could be Optional (bash $HOME/acoupi/src/acoupi/scripts/setup_microphone.sh)

11. Create directories to store audio files
sudo -u ${USER} mkdir -p acoupi/src/acoupi/storages/bats/recordings
sudo -u ${USER} mkdir -p acoupi/src/acoupi/storages/bats/detections
sudo -u ${USER} mkdir -p acoupi/src/acoupi/storages/no_bats/recordings
sudo -u ${USER} mkdir -p acoupi/src/acoupi/storages/no_bats/detections

9. Start service to run acoupi-batdetect2
bash $HOME/acoupi/src/acoupi/scripts/setup_service.sh